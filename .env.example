# 出血(mm): 分頁之前會裁切掉
PRINT_BLEED=2

# 頁面順序
PAGE_ORDER=3,4,1,5,6,2

# 壓縮設定
COMPRESS_IMAGES=true    # 圖片壓縮
IMAGE_QUALITY=85        # JPEG品質 (0-100)  
COMPRESS_PDF=true       # PDF結構壓縮
OUTPUT_DPI=150          # 輸出DPI (預設300，降低可大幅壓縮)